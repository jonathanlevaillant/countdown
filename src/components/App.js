import React from 'react';

import Layout from '../containers/Layout';
import Routing from '../containers/Routing';

const App = () => (
  <Layout>
    <Routing />
  </Layout>
);

export default App;
